# 기여 안내

1. 개인 PDF, 출력물, 캐시, 인증 파일을 커밋하지 않습니다.
2. 변경 전후에 `py -m pytest -q`를 실행합니다.
3. UI 변경은 노트북과 좁은 태블릿 화면에서 확인합니다.
4. 구조화 결과 모델이나 프롬프트를 바꾸면 캐시 버전도 검토합니다.
5. PR에는 재현 방법과 검증 결과를 적되 실제 강의자료 내용은 포함하지 않습니다.
